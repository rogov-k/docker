web:
  build: .
  ports:
    - "8000:80"
  links:
    - db
  volumes:
    - .:/code
db:
  image: "mysql:5"
  volumes:
    - ./mysql:/etc/mysql/conf.d
  environment:
    MYSQL_ROOT_PASSWORD: 1
    MYSQL_DATABASE: blog
    MYSQL_USER: admin
    MYSQL_PASSWORD: 38q8jd9f
